// Alias for the mobile app's `GET /users/check-username?username=` call.
// The canonical implementation lives at /api/auth/username-availability.
export { GET } from "../../auth/username-availability/route";
