// Static alias for the mobile app's `GET /posts/bookmarks` call.
// The handler in ../route detects the `/posts/bookmarks` pathname and serves
// the viewer's bookmarked posts.
export { GET } from "../route";
